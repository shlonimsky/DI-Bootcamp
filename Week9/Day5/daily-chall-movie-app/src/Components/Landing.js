import SearchForm from "./SearchForm"
import MovieContainer from "./MoviesContainer"


const Landing = (props) => {
    return (
        <div>
            <SearchForm />
            <MovieContainer />
        </div>
    )
}

export default Landing